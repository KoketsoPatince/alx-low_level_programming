#include <stdio.h>

/**
 * main - it is  the main function. the entry point
 *
 * Return: always = 0 when successful
 *
 */
int main(void)
{
int i;
for (i = 0; i < 10; i++)
printf("%d", i);
printf("\n");
return (0);

}
